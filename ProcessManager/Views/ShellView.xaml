<Window x:Class="ProcessManager.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ProcessManager.Views"
        Title="Manager Procesow - Artur Furmanczyk (155063) - PLANET KAiMS ETI PG"
        Width="1280" Height="720">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Label Background="Indigo" Content="Toolbar placeholder" />
        </StackPanel>
        <Grid Margin="10, 10, 10, 10" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <views:ProcessListView />
            <GridSplitter Width="5" Grid.Column="1" HorizontalAlignment="Stretch" />
            <ContentControl Grid.Column="2" Name="ActiveItem" />
        </Grid>
    </Grid>

</Window>